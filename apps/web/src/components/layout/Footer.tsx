export function Footer() {
  return (
    <footer style={{ padding: '1.5rem 0 2rem', color: 'var(--muted)' }}>
      <main>
        <small>OpenAgent scaffold for the contact management workflow.</small>
      </main>
    </footer>
  );
}